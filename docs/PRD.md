# skill-plan-lint PRD

## Summary

`skill-plan-lint` checks whether an agent skill file gives enough operational guidance to be reused safely: triggers, inputs, tools, side effects, approvals, examples, validation, limitations, and fallback behavior.

## Requirements

Local-only CLI, Markdown evidence line numbers, JSON and Markdown reports, fixture-backed tests, and smoke validation.
